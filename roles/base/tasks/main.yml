- name: Update
  ansible.builtin.apt:
    update_cache: yes

- name: Upgrade
  ansible.builtin.apt:
    upgrade: safe

- name: Install base packages
  ansible.builtin.package:
    name: "{{ item }}"
    state: present
  loop:
    - curl
    - sed
    - apache2-utils
    - sudo
    - exa